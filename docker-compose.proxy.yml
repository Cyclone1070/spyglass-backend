services:
  squid:
    build: ./relay
    ports:
      - "3128:3128"
    restart: always

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARED_TOKEN}
    restart: always
    depends_on:
      - squid
